component: midgardmvc_helper_location
version: 0.0.1
routes:
    userlocation_update:
        controller: midgardmvc_helper_location_controllers_userlocation
        action: location
        path: /mgd:userlocation
        root_only: true
        allowed_methods:
            - GET
            - POST
        mimetype: application/json
        template_aliases: 
            root: midgardmvc-show-json
