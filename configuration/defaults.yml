routes:
    userlocation_update:
        controller: org_routamc_positioning_controllers_userlocation
        action: location
        route: /mgd:userlocation
        root_only: true
        allowed_methods:
            - GET
            - POST
        mimetype: application/json
        template_entry_point: midcom-show-json
