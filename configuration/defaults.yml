routes:
    userlocation_update:
        controller: midgardmvc_helper_location_controllers_userlocation
        action: location
        route: /mgd:userlocation
        root_only: true
        allowed_methods:
            - GET
            - POST
        mimetype: application/json
        template_entry_point: midcom-show-json
